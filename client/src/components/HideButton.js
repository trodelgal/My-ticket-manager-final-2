import React, { useEffect, useState } from 'react';

function HideButton({ticketObject}){
   async function handleClick(){
        
    }

    return(
            <button onClick={handleClick}>hide</button>
    )
}

export default HideButton;